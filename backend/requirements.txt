fastapi==0.115.6
uvicorn[standard]==0.34.0
pandas==2.2.3
numpy==1.26.4
faker==33.1.0
duckdb==1.1.3
# The forecast engine's "Prophet" and "XGBoost" models are simplified
# stand-ins built on numpy and this one estimator — see services/forecast_engine.py.
# The real prophet/xgboost packages were pinned here but never imported, and
# prophet in particular pulls a Stan toolchain that dominates install time.
scikit-learn==1.6.0
pydantic==2.10.4
python-dotenv==1.0.1
httpx==0.28.1
