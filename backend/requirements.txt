# pyproject.toml is the source of truth for backend dependencies.
# This file is kept for tools/CI that still expect a requirements.txt.
# `pip install -r requirements.txt` will install the backend in editable
# mode, which transitively pulls every pinned dep from pyproject.toml.
-e .
